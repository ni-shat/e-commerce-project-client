/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.imgur.com', 'placeimg.com', 'i.ixxxxmgur.com' , 'cdn.dummyjson.com'], // Allow images from Imgur
    },
};

export default nextConfig;
